# LLM 与 Agent 岗位工程基础补丁题库

> 本文件用于补充 `INTERVIEW_QUESTIONS.md` 中相对较少覆盖的通用工程基础，不重复展开 Transformer、训练、RAG、Agent 原理、分布式训练和常规算法题。
>
> 题目面向 LLM 应用算法、Agent 算法、Agent 工程、模型服务和 AI 平台等岗位。重点不是背诵术语，而是能够解释机制、写出代码、定位故障并完成工程权衡。

## 使用说明

- `P0`：大多数 LLM/Agent 工程岗位都应掌握，建议能够结合代码或真实故障回答。
- `P1`：方向相关或常见追问，至少应理解核心机制和适用边界。
- 每道题使用唯一编号，答案可在独立目录中按编号维护。
- 本题库共 **100** 道题，建议与主问题总表配合使用，而不是并入同一复习清单。

## 题目统计

| 分类 | 题目数 |
|---|---:|
| Python 语言与运行时 | 10 |
| 并发、异步与任务调度 | 10 |
| 操作系统与 Linux | 10 |
| 计算机网络、HTTP 与 RPC | 10 |
| 数据库与数据存储 | 10 |
| 缓存、消息队列与分布式基础 | 10 |
| 服务设计、可靠性与安全 | 10 |
| Git、Docker 与 CI/CD | 10 |
| 软件设计、测试与代码质量 | 10 |
| 可观测性、性能分析与故障排查 | 10 |
| **总计** | **100** |

---

## 1. Python 语言与运行时

- [ ] `PYT-001` `P0` Python 中可变对象与不可变对象有什么区别，函数传参时实际传递的是什么？
- [ ] `PYT-002` `P0` 浅拷贝与深拷贝有什么区别，嵌套字典、列表和张量对象可能出现哪些问题？
- [ ] `PYT-003` `P0` Python 的 LEGB 作用域、闭包和 `nonlocal` 分别如何工作？
- [ ] `PYT-004` `P0` 迭代器、生成器和普通容器有什么区别，生成器如何降低大规模数据处理的内存占用？
- [ ] `PYT-005` `P0` 装饰器的本质是什么，如何实现一个同时支持同步和异步函数的计时或重试装饰器？
- [ ] `PYT-006` `P0` Context Manager 的进入与退出协议是什么，如何使用它管理模型、文件、锁和数据库连接？
- [ ] `PYT-007` `P0` Python 异常体系如何设计，什么时候应该捕获、包装、重新抛出或定义自定义异常？
- [ ] `PYT-008` `P1` Python 的引用计数、循环引用和垃圾回收如何工作，长期运行的 Agent 服务为什么仍可能发生内存泄漏？
- [ ] `PYT-009` `P1` `dataclass`、`NamedTuple`、普通类和 Pydantic Model 分别适合哪些数据建模场景？
- [ ] `PYT-010` `P1` Python 类型注解、泛型、Protocol 和静态类型检查能为大型 Agent 项目带来什么价值？

## 2. 并发、异步与任务调度

- [ ] `CON-001` `P0` 进程、线程和协程的调度方式、内存模型和适用场景有什么区别？
- [ ] `CON-002` `P0` Python GIL 是什么，它会限制哪些工作负载，又不会限制哪些工作负载？
- [ ] `CON-003` `P0` CPU-Bound 与 IO-Bound 任务应分别选择多进程、多线程还是 `asyncio`？
- [ ] `CON-004` `P0` `async`、`await`、Coroutine、Task 和 Event Loop 之间是什么关系？
- [ ] `CON-005` `P0` 异步代码中调用阻塞函数会发生什么，如何使用线程池、进程池或异步客户端修复？
- [ ] `CON-006` `P0` Race Condition、Deadlock、Livelock 和 Starvation 分别是什么，如何预防？
- [ ] `CON-007` `P0` Lock、RLock、Semaphore、Condition、Event 和 Queue 分别适合什么同步场景？
- [ ] `CON-008` `P0` 如何为并发工具调用实现超时、取消、结果聚合和部分失败处理？
- [ ] `CON-009` `P1` Backpressure 是什么，流式模型输出或批量任务系统如何避免生产者压垮消费者？
- [ ] `CON-010` `P1` 如何设计一个具有并发上限、优先级、公平性和可取消能力的异步任务调度器？

## 3. 操作系统与 Linux

- [ ] `OSL-001` `P0` 进程的虚拟地址空间通常包含哪些区域，栈、堆和内存映射分别存放什么？
- [ ] `OSL-002` `P0` 用户态与内核态有什么区别，一次系统调用大致经历哪些步骤？
- [ ] `OSL-003` `P0` 进程、线程和文件描述符在 Linux 中如何表示，为什么 Socket 也可以作为文件描述符操作？
- [ ] `OSL-004` `P0` Page Cache、Buffer Cache、Swap 和内存映射文件分别如何影响 IO 性能？
- [ ] `OSL-005` `P0` Linux 中 Zombie Process 与 Orphan Process 分别是什么，服务程序应如何正确回收子进程？
- [ ] `OSL-006` `P0` Signal 的作用是什么，如何让模型服务正确处理 `SIGTERM` 并完成优雅退出？
- [ ] `OSL-007` `P0` `top`、`htop`、`ps`、`free`、`df`、`du`、`lsof` 和 `ss` 分别用于排查什么问题？
- [ ] `OSL-008` `P1` Linux 文件权限、用户组、`umask` 和最小权限原则如何影响模型文件与密钥管理？
- [ ] `OSL-009` `P1` Cgroup 与 Namespace 分别隔离和限制什么资源，它们与容器是什么关系？
- [ ] `OSL-010` `P1` Load Average、CPU Utilization、IO Wait 和 Context Switch 应如何结合判断系统瓶颈？

## 4. 计算机网络、HTTP 与 RPC

- [ ] `NET-001` `P0` TCP/IP 分层模型中各层分别解决什么问题，一次 HTTP 请求会经过哪些主要步骤？
- [ ] `NET-002` `P0` TCP 三次握手与四次挥手分别解决什么问题，`TIME_WAIT` 为什么存在？
- [ ] `NET-003` `P0` TCP 如何通过序列号、确认、重传、流量控制和拥塞控制保证可靠传输？
- [ ] `NET-004` `P0` HTTP/1.1、HTTP/2 和 HTTP/3 在连接复用、队头阻塞和传输协议上有什么区别？
- [ ] `NET-005` `P0` Keep-Alive、连接池、DNS 缓存和 TLS Session Resumption 为什么会影响模型 API 延迟？
- [ ] `NET-006` `P0` HTTP 中 GET、POST、PUT、PATCH 和 DELETE 的语义与幂等性有何区别？
- [ ] `NET-007` `P0` SSE、WebSocket 和普通流式 HTTP 分别适合哪些大模型流式输出场景？
- [ ] `NET-008` `P0` Connect Timeout、Read Timeout、Write Timeout 和 Total Timeout 分别控制什么？
- [ ] `NET-009` `P1` REST、gRPC 和消息队列式通信在接口约束、性能和耦合程度上如何选择？
- [ ] `NET-010` `P1` 反向代理、负载均衡、API Gateway 和 Service Mesh 分别位于什么层级并承担什么职责？

## 5. 数据库与数据存储

- [ ] `DBS-001` `P0` 关系型数据库中的主键、唯一索引、普通索引和联合索引有什么区别？
- [ ] `DBS-002` `P0` B+ Tree 索引为什么适合数据库，联合索引的最左前缀原则是什么？
- [ ] `DBS-003` `P0` 事务的 ACID 分别表示什么，Agent 长任务状态更新为什么需要事务边界？
- [ ] `DBS-004` `P0` 脏读、不可重复读和幻读分别是什么，不同隔离级别如何处理？
- [ ] `DBS-005` `P0` MVCC 的基本思想是什么，它如何减少读写之间的阻塞？
- [ ] `DBS-006` `P0` 数据库连接池为什么必要，连接数过大或连接泄漏会产生什么后果？
- [ ] `DBS-007` `P0` 如何分析一条慢 SQL，执行计划中的全表扫描、回表和索引失效意味着什么？
- [ ] `DBS-008` `P1` 乐观锁与悲观锁分别适合什么并发冲突场景，版本号机制如何工作？
- [ ] `DBS-009` `P1` 关系型数据库、文档数据库、键值存储和对象存储分别适合保存 Agent 系统中的哪些数据？
- [ ] `DBS-010` `P1` 数据库 Schema 如何支持任务状态、事件日志、消息、工具调用和版本信息的可追溯性？

## 6. 缓存、消息队列与分布式基础

- [ ] `DST-001` `P0` Cache Aside、Read Through 和 Write Through 模式分别如何工作？
- [ ] `DST-002` `P0` 缓存穿透、击穿和雪崩分别是什么，常见治理方式有哪些？
- [ ] `DST-003` `P0` Redis 中 String、Hash、List、Set、Sorted Set 和 Stream 分别适合哪些场景？
- [ ] `DST-004` `P0` Redis 分布式锁应如何设计过期时间、唯一标识、续租和原子释放，为什么简单 `SETNX` 不够？
- [ ] `DST-005` `P0` 消息队列如何实现系统解耦、削峰、异步处理和失败重试？
- [ ] `DST-006` `P0` At-Most-Once、At-Least-Once 和 Exactly-Once 分别意味着什么，业务上如何实现幂等消费？
- [ ] `DST-007` `P0` 消息重复、乱序、积压和死信分别如何发生，应该如何处理？
- [ ] `DST-008` `P1` CAP 定理描述了什么，为什么网络分区发生时必须在一致性与可用性之间取舍？
- [ ] `DST-009` `P1` 强一致、最终一致、线性一致和因果一致分别适合哪些系统？
- [ ] `DST-010` `P1` 分布式系统中的时钟偏差、唯一 ID 和事件顺序为什么困难，常见解决思路有哪些？

## 7. 服务设计、可靠性与安全

- [ ] `SVC-001` `P0` 什么是幂等性，创建任务、发送消息和执行支付等接口应如何设计幂等键？
- [ ] `SVC-002` `P0` 重试为什么可能放大故障，指数退避、随机抖动和重试预算分别解决什么问题？
- [ ] `SVC-003` `P0` Timeout、Retry、Circuit Breaker、Rate Limit 和 Bulkhead 如何组成服务韧性机制？
- [ ] `SVC-004` `P0` 令牌桶与漏桶限流算法有什么区别，如何同时支持租户配额和全局限流？
- [ ] `SVC-005` `P0` 如何设计模型服务或 Agent 服务的优雅降级，例如切换模型、减少工具、关闭非核心功能或返回缓存？
- [ ] `SVC-006` `P0` 鉴权与授权有什么区别，API Key、Session、JWT、OAuth 2.0 和 RBAC 分别适合什么场景？
- [ ] `SVC-007` `P0` 如何安全管理模型平台中的密钥、Token、数据库密码和第三方工具凭证？
- [ ] `SVC-008` `P0` SSRF、命令注入、路径遍历、反序列化和依赖供应链攻击如何威胁 Agent 工具系统？
- [ ] `SVC-009` `P1` 多租户系统如何实现身份隔离、数据隔离、资源配额、审计和成本归属？
- [ ] `SVC-010` `P1` RTO、RPO、SLA、SLO 和 Error Budget 分别是什么，如何用于制定服务可靠性目标？

## 8. Git、Docker 与 CI/CD

- [ ] `DEV-001` `P0` Git 的 Working Tree、Index、Commit 和 Branch 分别是什么，`add` 与 `commit` 实际完成什么操作？
- [ ] `DEV-002` `P0` `merge`、`rebase`、`cherry-pick` 和 `revert` 分别适合什么场景？
- [ ] `DEV-003` `P0` 如何处理代码冲突、误提交密钥、错误合并和已经推送的错误 Commit？
- [ ] `DEV-004` `P0` Docker Image、Container、Layer、Volume 和 Network 分别是什么？
- [ ] `DEV-005` `P0` 如何编写体积较小、构建稳定且安全的 Python 模型服务 Dockerfile？
- [ ] `DEV-006` `P0` 容器中的端口、环境变量、挂载目录、GPU 设备和文件权限应如何配置？
- [ ] `DEV-007` `P1` Docker Compose、Kubernetes Deployment、Service 和 ConfigMap/Secret 分别解决什么问题？
- [ ] `DEV-008` `P1` Readiness Probe、Liveness Probe 和 Startup Probe 有何区别，模型加载较慢时应如何配置？
- [ ] `DEV-009` `P1` CI 流水线通常应包含哪些代码检查、测试、构建和安全扫描步骤？
- [ ] `DEV-010` `P1` 蓝绿发布、滚动发布、金丝雀发布和 Feature Flag 分别如何降低上线风险？

## 9. 软件设计、测试与代码质量

- [ ] `SWE-001` `P0` 高内聚、低耦合、单一职责和依赖倒置在 Agent Runtime 中应如何体现？
- [ ] `SWE-002` `P0` 接口、抽象基类、组合和继承应如何选择，为什么工程中通常优先组合？
- [ ] `SWE-003` `P0` Adapter、Strategy、Factory、Observer 和 State Pattern 分别适合 Agent 系统中的哪些组件？
- [ ] `SWE-004` `P0` 单元测试、集成测试、端到端测试和回归测试分别验证什么？
- [ ] `SWE-005` `P0` Mock、Stub、Fake 和 Spy 有什么区别，测试模型 API 与外部工具时应如何使用？
- [ ] `SWE-006` `P0` 如何测试具有随机输出、网络依赖和异步执行的大模型应用？
- [ ] `SWE-007` `P0` Property-Based Testing、Golden Test 和 Snapshot Test 分别适合验证什么？
- [ ] `SWE-008` `P1` 如何设计向后兼容的配置、事件协议、数据库 Schema 和工具接口？
- [ ] `SWE-009` `P1` 如何通过代码审查识别过度设计、隐藏副作用、异常吞噬和资源泄漏？
- [ ] `SWE-010` `P1` 如何建立格式化、Lint、类型检查、测试覆盖率和依赖锁定的统一质量门禁？

## 10. 可观测性、性能分析与故障排查

- [ ] `OBS-001` `P0` Log、Metric、Trace 和 Profile 分别回答什么问题，为什么不能只依赖日志？
- [ ] `OBS-002` `P0` 如何设计结构化日志，并使用 Request ID、Trace ID、Task ID 和 User/Tenant ID 串联一次 Agent 调用？
- [ ] `OBS-003` `P0` RED、USE 和 Golden Signals 分别从什么角度监控服务？
- [ ] `OBS-004` `P0` P50、P95、P99 延迟分别说明什么，为什么平均延迟可能掩盖线上问题？
- [ ] `OBS-005` `P0` 如何区分 CPU、内存、磁盘 IO、网络、数据库、外部 API 和 GPU 导致的性能瓶颈？
- [ ] `OBS-006` `P0` 内存持续增长时，应如何区分缓存增长、对象泄漏、连接泄漏、Tensor 未释放和进程碎片？
- [ ] `OBS-007` `P0` 服务出现间歇性超时时，应该按照什么顺序检查客户端、网络、网关、线程池、连接池和下游服务？
- [ ] `OBS-008` `P0` Agent 任务成功率下降时，如何利用 Trace 将问题归因到模型、上下文、工具、环境或状态管理？
- [ ] `OBS-009` `P1` 压测中的并发数、QPS、吞吐、响应时间和资源利用率是什么关系，如何避免把压测工具本身变成瓶颈？
- [ ] `OBS-010` `P1` 如何建立故障复盘，包括影响范围、时间线、根因、修复、预防措施和后续验证？
